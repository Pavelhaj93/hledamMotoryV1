"use client";

import Button from "@/components/Button";
import React from "react";

const BackToWebButton = () => {
  return (
    <Button color="primary" center>
      Zpátky na web
    </Button>
  );
};

export default BackToWebButton;
